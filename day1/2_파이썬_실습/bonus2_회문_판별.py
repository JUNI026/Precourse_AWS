# =====================================================
# 파이썬 실습 - 심화 미션 2. 회문 판별 (코딩테스트 연습용 보너스)
# =====================================================
# 문자열이 앞으로 읽어도 뒤로 읽어도 같은지(회문) 판별하는 함수를 작성하세요.
# 단, 뒤집기 슬라이싱([::-1])을 쓰지 않고 반복문으로 구현해 보세요.
#
# 🤔 힌트
#   1. 앞에서 i번째 글자와 뒤에서 i번째 글자를 짝지어 비교합니다.
#   2. 뒤에서 i번째 글자는 text[len(text) - 1 - i] 로 나타낼 수 있습니다.
#   3. 비교는 문자열의 절반까지만 해도 됩니다!
#   4. 문자열의 절반은 len(text) // 2 로 나타낼 수 있습니다.

def is_palindrome(text):
    # TODO: 수업 코드를 따라 여기에 작성해 보세요.
    pass

print(is_palindrome("level"))    # True가 나와야 합니다
print(is_palindrome("hello"))    # False가 나와야 합니다
